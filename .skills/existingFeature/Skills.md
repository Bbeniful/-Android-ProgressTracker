### Working with existing feature

- You will get tasks to add or fix a new functions/UI elements/tests for existing feature
- In order to follow the existing pattern follow the skills in `.skills/sub` folders
- if the logic the user wants not clear, you need to ask questions until it is not clear 100%